{
  "name": "playwright-cucumber-gherkin",
  "version": "1.0.0",
  "description": "Playwright automation with Cucumber/Gherkin",
  "main": "index.js",
  "scripts": {
    "test": "npx bddgen && playwright test",
    "test:tag": "npx bddgen && playwright test --grep",
    "test:report": "npx bddgen && playwright test && npm run report:generate",
    "test:full": "npx bddgen && playwright test --grep @api && npm run report:generate",
    "test:positive": "npx bddgen && playwright test jsonplaceholder-api.feature && npm run report:generate",
    "test:negative": "npx bddgen && playwright test --grep @negative && npm run report:generate",
    "playwright:install": "playwright install",
    "report:generate": "node reporting/generate-report.js"
  },
  "keywords": [
    "playwright",
    "cucumber",
    "gherkin",
    "bdd",
    "testing"
  ],
  "devDependencies": {
    "@playwright/test": "^1.40.1",
    "@types/node": "^20.10.6",
    "multiple-cucumber-html-reporter": "^3.8.0",
    "playwright-bdd": "^7.0.0",
    "typescript": "^5.3.3"
  }
}
